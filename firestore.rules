rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // In production this needs strict rules, but for this demo/prototype it allows P2P network.
    }
  }
}
